/**
 * Stub: replace with Resend, Supabase Edge Function, or transactional provider.
 */
export async function sendEmailNotification(
  customerEmail: string,
  receipt: { orderId: string; body: string }
): Promise<void> {
  void customerEmail;
  void receipt;
}
